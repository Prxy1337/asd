-- =====================================================
-- 🧹 UI CLEANUP
-- =====================================================
pcall(function()
	for _, gui in ipairs(game.CoreGui:GetChildren()) do
		if gui:IsA("ScreenGui") and gui.Name:lower():find("lux") then
			gui:Destroy()
		end
	end
end)

-- =====================================================
-- 📦 LUXWARE UI
-- =====================================================
local Luxware = loadstring(game:HttpGet(
	"https://raw.githubusercontent.com/xHeptc/Luxware-UI-Library/main/Source.lua"
))()

local Window = Luxware.CreateWindow("卐Führer Script卐", 15275687632)

-- =====================================================
-- 📑 TABS
-- =====================================================
local EventTab     = Window:Tab("🎯 500k Event")
local ValentineTab = Window:Tab("❤️ Valentine")
local HalloweenTab = Window:Tab("🎃 Halloween")
local CreditsTab   = Window:Tab("📜 Credits")

CreditsTab:Section("Credits"):Label("Script by Hitler卐")

-- =====================================================
-- 👤 SERVICES
-- =====================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

-- =====================================================
-- ⚙️ SETTINGS
-- =====================================================
local WALK_SPEED = 60
local STAND_DELAY = 0.20
local EVENT_WAIT = 1.0
local TREE_WAIT  = 0.35

-- =====================================================
-- 📦 SHARDS SPAWNERS
-- =====================================================
local function scanSpawners()
	local positions = {}
	local content = workspace:WaitForChild("__GAME_CONTENT")
	local folder = content:WaitForChild("ShardsSpawners")
	for _, obj in ipairs(folder:GetDescendants()) do
		if obj:IsA("BasePart") then
			table.insert(positions, obj.Position)
		end
	end
	return positions
end

local POSITIONS = scanSpawners()

-- =====================================================
-- 🔍 NEAREST
-- =====================================================
local function getNearest(pos)
	local index, dist = nil, math.huge
	for i, p in ipairs(POSITIONS) do
		local d = (p - pos).Magnitude
		if d < dist then
			dist = d
			index = i
		end
	end
	if index then
		return table.remove(POSITIONS, index)
	end
end

-- =====================================================
-- 🧱 NOCLIP
-- =====================================================
local noclipConn
local function enableNoclip()
	noclipConn = RunService.Stepped:Connect(function()
		local char = player.Character
		if not char then return end
		for _, p in ipairs(char:GetDescendants()) do
			if p:IsA("BasePart") then
				p.CanCollide = false
			end
		end
	end)
end

local function disableNoclip()
	if noclipConn then
		noclipConn:Disconnect()
		noclipConn = nil
	end
end

-- =====================================================
-- 🚶 SMOOTH WALK (500k FARM)
-- =====================================================
local function smoothWalkTo(targetPos)
	local char = player.Character
	if not char then return end
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	while (hrp.Position - targetPos).Magnitude > 2 do
		local dir = (targetPos - hrp.Position).Unit
		hrp.CFrame = hrp.CFrame + dir * WALK_SPEED * task.wait()
	end
end

-- =====================================================
-- 🔁 STATES
-- =====================================================
local farming = false
local upgradingEvent = false
local upgradingValentine = false
local upgradingLove = false
local upgradeTreeOn = false
local autoRebirth = false
local upgradingHalloween = false
local autoHalloweenRebirth = false
local autoCandy = false
local upgradingCandyCorn = false

-- =====================================================
-- 🔼 REMOTE
-- =====================================================
local MainRemote = ReplicatedStorage
	:WaitForChild("Packages")
	:WaitForChild("Knit")
	:WaitForChild("Services")
	:WaitForChild("RemotesService")
	:WaitForChild("RE")
	:WaitForChild("MainRemote")

-- =====================================================
-- 🔁 AUTO FARM 500k
-- =====================================================
task.spawn(function()
	while true do
		if farming then
			if #POSITIONS == 0 then
				POSITIONS = scanSpawners()
			end
			local char = player.Character
			local hrp = char and char:FindFirstChild("HumanoidRootPart")
			if hrp then
				local target = getNearest(hrp.Position)
				if target then
					smoothWalkTo(target)
					task.wait(STAND_DELAY)
				end
			end
		end
		task.wait(0.05)
	end
end)

-- =====================================================
-- 🔼 EVENT UPGRADES 500k
-- =====================================================
local EVENT_UPGRADES = {
	"Event Shards",
	"Event Shards 2",
	"Event Luck",
	"Event Bulk",
	"Event Crystals",
	"Event Crystal Chance"
}

task.spawn(function()
	while true do
		if upgradingEvent then
			for _, upg in ipairs(EVENT_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeAll", "Event Shards", upg)
				end)
				task.wait(EVENT_WAIT)
			end
		end
		task.wait(0.5)
	end
end)

-- =====================================================
-- 🌳 AUTO UPGRADE TREE (LANGSAM & STABIL)
-- =====================================================
local TREE_UPGRADES = {
	"Event1_Start",
	"Event1_Shards1",
	"Event1_Shards2",
	"Event1_EventLuck1",
	"Event1_EventLuck2",
	"Event1_EventBulk",
	"Event1_Shards3",
	"Event1_Shards4"
}

task.spawn(function()
	while true do
		if upgradeTreeOn then
			for _, name in ipairs(TREE_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeTree", name)
				end)
				task.wait(TREE_WAIT)
			end
		end
		task.wait(0.6)
	end
end)

-- =====================================================
-- 🔁 AUTO REBIRTH 500k
-- =====================================================
task.spawn(function()
	while true do
		if autoRebirth then
			pcall(function()
				MainRemote:FireServer("Event1_Ascend")
			end)
			task.wait(2)
		end
		task.wait(0.5)
	end
end)

-- =====================================================
-- ❤️ VALENTINE UPGRADES
-- =====================================================
local VALENTINE_UPGRADES = {
	"Infinite Hearts",
	"Valentine Bulk",
	"Valentine Shard Chance",
	"Valentine Infinite Love",
	"Valentine Boost"
}

task.spawn(function()
	while true do
		if upgradingValentine then
			for _, upg in ipairs(VALENTINE_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeAll", "Hearts", upg)
				end)
				task.wait(1.2)
			end
		end
		task.wait(0.6)
	end
end)

local LOVE_UPGRADES = {
	"Hearts","Love","Clicks","Clicker Tokens",
	"Fish Multiplier","Light Points","Madness",
	"Sins","Sunite","Rune Bulk","Rune Luck",
}

task.spawn(function()
	while true do
		if upgradingLove then
			for _, upg in ipairs(LOVE_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeAll", "Love", upg)
				end)
				task.wait(1.2)
			end
		end
		task.wait(0.6)
	end
end)

-- =====================================================
-- 🎃 HALLOWEEN MOBS (TP WIEDER DRIN!)
-- =====================================================
local HALLOWEEN_MOBS = {
	{key="Clue", label="Clue 10"},
	{key="Lucky", label="Lucky 90"},
	{key="Popebaki", label="Popebaki 700"},
	{key="Ninia", label="Ninia 5K"},
	{key="Pixelated", label="Pixelated 45K"},
	{key="Nuke", label="Nuke 385K"},
	{key="Blizz", label="Blizz 3.25M"},
	{key="Yoshy", label="Yoshy 30M"},
	{key="Christmasnobo", label="Christmasnobo 250M"},
	{key="Dirtyeyes", label="Dirtyeyes 1.75B"},
	{key="Lorax", label="Lorax 15B"},
	{key="Zyfley", label="Zyfley 120B"},
	{key="Matis", label="Matis 1T"},
	{key="15k", label="15k 15T"},
	{key="Connor", label="Connor BOSS"},
	{key="0hxsn", label="0hxsn BOSS"},
	{key="Ghoulax", label="Ghoulax BOSS"},
}

local BOSS = {["0hxsn"]=true,["Connor"]=true,["Ghoulax"]=true}
local activeMobs = {}
local mobIndex = {}

local function getMobTargets(mobName)
	local results = {}
	local folder = workspace:WaitForChild("__GAME_CONTENT"):WaitForChild("Mobs"):WaitForChild("Halloween")
	for _, model in ipairs(folder:GetChildren()) do
		if model:IsA("Model") and model.Name == mobName then
			local part = model:FindFirstChild("HumanoidRootPart") or model:FindFirstChildWhichIsA("BasePart")
			if part then table.insert(results, part) end
		end
	end
	return results
end

task.spawn(function()
	while true do
		for mobName, enabled in pairs(activeMobs) do
			if enabled then
				local targets = getMobTargets(mobName)
				if #targets > 0 then
					mobIndex[mobName] = (mobIndex[mobName] or 0) + 1
					if mobIndex[mobName] > #targets then mobIndex[mobName] = 1 end
					local target = targets[BOSS[mobName] and 1 or mobIndex[mobName]]
					local char = player.Character
					local hrp = char and char:FindFirstChild("HumanoidRootPart")
					if hrp and target then
						hrp.CFrame = target.CFrame * CFrame.new(0,0,-4)
					end
				end
				task.wait(BOSS[mobName] and 0.7 or 0.35)
			end
		end
		task.wait(0.15)
	end
end)

-- =====================================================
-- 🎃 HALLOWEEN UPGRADES + REBIRTH
-- =====================================================
local HALLOWEEN_UPGRADES = {
	"Flesh Multiplier",
	"Flesh Multiplier II",
	"Mob Damage Multiplier",
	"Attack Speed",
	"Halloween Bulk",
	"Halloween Luck",
	"Sword Enchants Gems Chance",
	"Candy Corn Multiplier III",
	"Unlock Candy Corn",
	"Refined Candy Multiplier III",
	"Factorized Candy Multiplier V",
}

task.spawn(function()
	while true do
		if upgradingHalloween then
			for _, upg in ipairs(HALLOWEEN_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeAll", "Flesh", upg)
				end)
				task.wait(0.9)
			end
		end
		task.wait(0.5)
	end
end)

task.spawn(function()
	while true do
		if autoHalloweenRebirth then
			pcall(function()
				MainRemote:FireServer("HalloweenEvent_Fleshify")
			end)
			task.wait(2.5)
		end
		task.wait(0.5)
	end
end)

-- =====================================================
-- 🍬 AUTO CANDY PULL (STABIL)
-- =====================================================
local function getCandyFolder()
	local content = workspace:WaitForChild("__GAME_CONTENT")
	local playersCandy = content:WaitForChild("PlayersCandy")
	return playersCandy:FindFirstChild(tostring(player.UserId))
end

task.spawn(function()
	while true do
		if autoCandy then
			local folder = getCandyFolder()
			if folder then
				local char = player.Character
				local hrp = char and char:FindFirstChild("HumanoidRootPart")
				if hrp then
					for _, obj in ipairs(folder:GetChildren()) do
						local part = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart")
						if part then
							part.CFrame = hrp.CFrame * CFrame.new(0,0,-2)
						end
					end
				end
			end
		end
		task.wait(0.2)
	end
end)

-- =====================================================
-- 🍬 CANDY CORN UPGRADES (NEU)
-- =====================================================
local CANDY_CORN_UPGRADES = {
	"Pickup Radius",
	"Candy Corn Multiplier",
	"Candy Corn Spawn Rate",
}

task.spawn(function()
	while true do
		if upgradingCandyCorn then
			for _, upg in ipairs(CANDY_CORN_UPGRADES) do
				pcall(function()
					MainRemote:FireServer("UpgradeAll", "Candy Corn", upg)
				end)
				task.wait(1.2)
			end
		end
		task.wait(0.6)
	end
end)

-- =====================================================
-- 🖥️ UI
-- =====================================================
local EventSection = EventTab:Section("500k Event")
EventSection:Toggle("Auto Shards Farm", function(v) farming=v if v then enableNoclip() else disableNoclip() end end)
EventSection:Toggle("Auto Upgrade Event", function(v) upgradingEvent=v end)
EventSection:Toggle("Auto Upgrade Tree", function(v) upgradeTreeOn=v end)
EventSection:Toggle("Auto Rebirth", function(v) autoRebirth=v end)

local ValentineSection = ValentineTab:Section("Valentine Event")
ValentineSection:Toggle("Auto Upgrade Valentine", function(v) upgradingValentine=v end)
ValentineSection:Toggle("Auto Upgrade Love", function(v) upgradingLove=v end)

local HalloweenSection = HalloweenTab:Section("👻 Mob Teleport")
for _, mob in ipairs(HALLOWEEN_MOBS) do
	HalloweenSection:Toggle("TP: "..mob.label, function(state)
		activeMobs[mob.key] = state
	end)
end

local UpgradeSection = HalloweenTab:Section("🛠️ Halloween Upgrade & Rebirth")
UpgradeSection:Toggle("Auto Upgrade Halloween", function(v) upgradingHalloween=v end)
UpgradeSection:Toggle("Auto Rebirth Halloween", function(v) autoHalloweenRebirth=v end)
UpgradeSection:Toggle("Auto Collect Candy Corn", function(v) autoCandy=v end)
UpgradeSection:Toggle("Auto Candy Corn Upgrades", function(v) upgradingCandyCorn=v end)
